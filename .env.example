# Copy this file to .env for local development, and configure the same
# variables as Environment Variables / Secrets in Cloudflare Pages for
# production (Settings -> Environment variables).

# Required to activate real email delivery in src/pages/api/contact.ts
# (see the TODO block in that file). Get a key at https://resend.com
RESEND_API_KEY=
